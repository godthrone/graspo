"""Tests for ``graspo.backends.graspoflow.trainer.rollout`` — RolloutMixin."""


def test_rollout_module_is_importable():
    """Verify the rollout mixin module is importable."""
    from graspo.backends.graspoflow.trainer.rollout import RolloutMixin  # noqa: F401
