# Example preset only. GRASPO itself is model-agnostic.
model:
  trust_remote_code: true
  chat_template_kwargs:
    enable_thinking: false

lora:
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj

reward:
  check_think: false
  check_json_markdown: true

